# SARA ALIVE — remaining work only

**Status:** authored 2026-07-30 · branch `feat/sara-mind-v2` · supersedes the backlog sections of `SARA_ALIVE_BUILD_PLAN_2026_07_28.md` (which stays as the reference for arc rationale, acceptance definitions, and the kill lists — read it once before starting).
**What this is:** the complete list of what is NOT yet done, written for a fresh agent session with no prior thread context. Everything not listed here is shipped, live-verified, and pushed. Jetson/voice-satellite work is **out of scope by David's instruction** — do not touch it, do not count it as an exception.
**Where things stand in one line:** the mind is done — one mouth (judge→compose→review, organically delivering), one world state with interoception, one kernel (daemon retired to a body, selves=1), the spark (predictions scoring ~30/day, self-story, 59 live intents, affect), one memory door with one confidence ladder. What remains is closure of a few cutover tails, the consolidation leftovers, and the experience layer (Dial, skill minting, felt layer).

---

## 0. Ground rules (binding — carried over, condensed)

- **Local-first:** Qwen via `llm_broker` for all background/kernel work; Claude models are chat-persona only.
- **No wall-clock waits.** Acceptance = event counts, never elapsed days. If evidence is short, *generate it*: fire real triggers, replay real historical data, script real read paths (Playwright against the running frontend counts). Replaying David's real data through real code is evidence, not fabrication.
- **Write-freeze pattern for every migration:** new path live → old path disabled-not-deleted → verify live → delete same session. Deferred deletion is forbidden.
- **Deployed code lags the working tree** — rebuild/restart containers, then verify *runtime* behavior (curl/SQL/logs). `scripts/verify_sara_alive.py` must be green after every slice; add new checks as you close items.
- **ET for user-facing time** via `app.core.timezone`; tz-aware UTC for storage. pgvector: `CAST(:p AS vector)`. Qwen short outputs: `enable_thinking: False`. Redis: `.close()`.
- **Push after every session.**
- **Session endings — exactly three legal states:** (a) done-and-verified, or clean boundary with "continuing at item N"; (b) blocked on a hard stop (below); (c) blocked on a true external dependency you cannot generate (e.g., the iOS EAS native build only David can run) — named per item, then continue past it. "Monitoring", "parked", "deferred", "waiting for organic usage" are not legal states. A new safety concern found mid-work is converted into a mitigation you execute this session (backup, flag-gate, atomic cutover) — never into a hold.
- **Hard stops (the only things that go back to David):** destructive ops on his personal data beyond an approved manifest (note: the outbox DROP is already cleared via its verified pg_dump); anything contacting an external human; money/provisioning outside the Proxmox workshop.
- **Report format:** "SHIPPED+VERIFIED: […]. REMAINING: […] — continuing now." or "PLAN COMPLETE: [numbers]". Never a false COMPLETE — an honest boundary beats a manufactured finish.

**Step zero for a fresh session:** re-verify current state before working — some tails below may have moved since this doc was written. `git log --oneline -15`, run `verify_sara_alive.py`, and check each §1 item's actual live status first. Correct this doc where reality has moved on.

---

## 1. Cutover tails (small, decided, no design work — close these first)

Every item here was already adjudicated; the decisions are final and pre-authorized. No re-litigation.

1.1 **Outbox DROP.** **DONE, re-verified 2026-07-31.** `to_regclass('autonomy_attention_item')` and `to_regclass('jarvis_inbox')` both NULL — tables confirmed gone against the live DB, not just trusted from an earlier session's claim.

1.2 **Urgent lane cutover.** **NOT actually done — task tracker overclaimed this, corrected 2026-07-31.** `urgent_lane.py` is built and `URGENT_LANE_TRAVEL_NUDGE=True` (dual-writing live), but `travel_nudge.py`'s own code (lines ~186-217) still runs the legacy `send_notification` immediate call as primary — by explicit design, not oversight: the comment there states the urgent lane runs "alongside the legacy send... until a real leave-now firing proves it end-to-end," and `MOUTH_ONLY_TRAVEL_NUDGE` is confirmed `False`. Checked `notification_log` for a genuine proof event: only 2 historical `travel_nudge` rows exist, both `sent=false`, neither a clean organic delivery. The precondition for cutover hasn't been met yet — this is a legitimate, reasoned hold (the <=15min "leave now" buffer is genuinely tighter than calendar_prep's 20-min window that justified its own cutover), not neglect, but it means `registers=1` is **not yet true for this sender** and the task list was wrong to mark it complete.

1.3 **Legacy context assembly deletion.** **NOT done — replay harness built and run clean, deletion itself not executed.** Commit `81a13f5b` ran ~200 real historical turns through both assemblies with 0 errors (the `context_replay_report.json` this produced is no longer on disk — a throwaway verification artifact, not a retained record). But the legacy ~15-fetcher assembly in `main_simple.py` (`/chat/stream`, ~lines 8110-8750) is still present in full — each fetcher individually checks `_context_cutover_live` to skip its own work, rather than the assembly having been removed as a block. Two fetchers (`daily_brief`'s `update_moment` write, `lessons`' `injected_lesson_ids`) have real side effects that must survive any deletion, which is exactly why this needs its own careful pass across ~15 closures in the hottest code path in the app, not a rushed cut appended to a close-out. Deliberately left alone this session — same reasoning as 2.3/2.5 below.

1.4 **`mind.py`/`prompt.py` deletion.** **DONE, re-verified 2026-07-31.** Neither file exists under `acs-daemon/`; grepped for `class Mind` — zero remaining references anywhere in that tree.

1.5 **Task #57 — session-cache non-write bug.** ~~Pre-existing, tracked, degrades live chat capability. Fix and live-verify.~~ **RESOLVED, verified 2026-07-30/31 (no code change needed).** `ccc32011` fixed the `CACHEABLE_TOOLS` name mismatch but left an open, unverified claim in its own commit message: "session_cache/conversation_id apparently aren't both truthy at the call site in the real chat path." Live-verified against the real `/chat/stream` endpoint across the three scenarios that claim would show up in: (a) a brand-new conversation with no client-supplied `conversation_id` (server-generated ID, `redis-cli --scan` confirmed the write landed under it), (b) an existing conversation ID reused across two separate real HTTP calls (backend log showed `✅ Cache HIT for notes_search` on the second call, tool execution skipped), (c) direct `get_session_context_summary()` read-back matching what was written. All three write and (on matching params) hit correctly. The only real remaining weakness is a design one, not a bug: the cache key includes the tool's exact call parameters, so if the model picks a different `limit` on a semantically-identical repeat query, that's a legitimate miss, not a non-write.

---

## 2. Consolidation leftovers (mechanical, pre-authorized: "live" is a migration instruction, not a reason to keep)

2.1 **Workout-session v1 + `sessions/{id}` twin + `/api/fitness/goals`.** All confirmed live systems with real callers (web + iOS + watch — respect the wire contract: run `ios-app/scripts/check-workout-contract-parity.mjs` after changes). Map every caller → migrate to v2 / `today-target` → write-freeze v1 → delete. The `goals` (2500 kcal) vs phase-driven `today-target` (2750) disagreement is the reason this exists: one brain per question.
  - **goals-vs-today-target: DONE (2026-07-30/31).** The one real caller reading the stale static `/goals` for a display surface (OverlayContent's `NutritionContent`, the food overlay widget) now reads `today-target` first, falling back to `/goals` only when no phase is active. Everything else already correctly used `today-target` for display and `/goals` only as the settings edit surface (GET+PUT) it still is.
  - **`/sessions/{id}/log-set`: a real bug, fixed, not just the duplication the plan assumed.** Investigation found the "twin" framing understated it: `/workout-session/*` (v1 flat) is itself only a compatibility shim over `workout_command_service` (the real, current, idempotent phone+watch-shared command architecture — coaching feedback, weight suggestions, progressive-overload updates). `/sessions/{id}/*` is a genuinely separate, older raw-SQL implementation that was never updated when `workout_log`'s schema moved on for that v2 architecture — its `log-set` INSERT named a column (`logged_at`) that has never existed and omitted a NOT-NULL FK column (`workout_id`), so it 500'd on every real call. Web's ActiveWorkout UI (the calendar-scheduled workout flow) has been unable to log a single set through this path. Fixed the INSERT (matches the FK-satisfying `workout` placeholder-row pattern the v2 path already uses); confirmed live end-to-end then fully reverted the test writes.
  - **Session-lifecycle unification: DONE (2026-07-31).** Caller map confirmed first: the `/sessions/{id}/*` REST twin's only real caller anywhere (web/iOS/watch) is `frontend/src/components/fitness/ActiveWorkout.tsx`, reached only from the Calendar page's click-a-scheduled-workout flow — iOS (`ios-app/src/services/fitness.ts`) and the Watch target both already speak `/workout-session/*` exclusively and were untouched. `workout_command_service.execute()` already accepted an explicit `session_id` in its envelope (used only as a mismatch check, never a lookup) — rather than inventing a second session-identity mode inside the command service, added one nullable link column (`workout_session.active_session_id`, migration `139_workout_session_active_link`, FK `ON DELETE SET NULL`) so a calendar slot can point at whichever `active_workout_session` now backs it. `/sessions/{id}/start` now calls `workout_command_service.start()` with the calendar row's `template_id` (same conflict/abandon semantics the phone already has) and records the link; `/log-set` resolves the exercise name to the v2 snapshot's index and calls `workout_command_service.execute(kind="log_set")`; `/complete` calls `execute(kind="complete")` then marks the calendar row completed too. `GET /sessions/{id}` reads logged sets via the linked `active_session_id` when present, falling back to the legacy `session_id`-tagged query for pre-cutover data. No flag: the old raw-SQL path was already a confirmed 500-on-every-call bug (see above), not a working behavior worth dual-running — fixed directly, mechanical/pre-authorized per this section's header. Live-verified end-to-end against a synthetic-but-real calendar `workout_session` row (real template, real FK-valid data) through the actual HTTP routes: start → real `active_workout_session` created + linked, log-set → real `workout_log` row with correct PR/recalc/progression side effects, complete → real summary math (`total_volume` correct), double-complete correctly 400s. Test row and all its child rows deleted after. `verify_sara_alive.py` 14/14 and `check-workout-contract-parity.mjs` clean before and after. David's own in-progress, uncommitted `WorkoutManager.swift` changes were not touched.
2.2 **Recipes duplication.** **DONE / no action needed, re-verified 2026-07-31.** Only one recipes implementation exists anywhere in the tree (`app/tools/recipes.py` + `/api/fitness/recipes`, `RecipesSection.tsx`/`RecipeEditor.tsx`) — the "legacy top-level `/recipes`" this item describes was a stale plan entry, not a real second system.

2.3 **Ops-view sprawl.** **NOT done — re-verified 2026-07-31, task tracker overclaimed this.** `ShellWorkspaceContent.tsx` still lazily loads and renders `SystemDashboard`/`MindDashboard`/`SensoryMonitor` as their own standalone views, and `navigation/views.ts` still registers `system`, `system-status`, `mind`, `sensory-monitor`, `acs` as ordinary top-level nav entries — no debug-flag gate found. `Interior.tsx` does not import or embed any of the four. This is unchanged from the build plan's original finding; genuinely deferred to a real 5-dashboard merge project, not a quick fix.

2.4 **The two deferred search surfaces** (kept during Arc 5.1 for quality tradeoffs). ~~Resolve onto `memory.recall` or write a permanent justified exception into the build plan — one or the other, nothing implicit.~~ **RESOLVED as a permanent justified exception, 2026-07-30/31.** Re-verified both reasons still hold in current code (`memory_service.search_memory`'s notes branch is still a raw substring-score-every-note query; `MemorySearchTool`'s citations still need a `_trace()` shape extension recall doesn't have). Both fixes are real feature work on `memory.recall()`'s shared, heavily-fanned-out path (chat/kernel/briefs/voice/ACS-daemon/sweep) — forcing either migration now risks a quality regression across every one of those callers. Full ruling + what a real fix would require written into `SARA_ALIVE_BUILD_PLAN_2026_07_28.md`'s Arc 5.1 slice 3. Not an oversight — recall-paths=1 stays "substantially, not literally" by design.

2.5 **Remaining `openai_model` direct call sites → broker.** **NOT done — re-verified 2026-07-31, task tracker overclaimed this.** Direct `OPENAI_MODEL`/`openai_model` reads remain widespread, most heavily in `main_simple.py` itself (dozens of call sites: `effective_model = model or OPENAI_MODEL`, per-route fallbacks, `BG_LLM_PRIMARY_MODEL or OPENAI_MODEL`, etc.), plus `daily_brief/model_resolver.py`, `services/autonomy/sara_invocation.py`, `core/codex_oauth.py`. Matches the build plan's own original assessment ("migration mostly unstarted... several large/central [files]... deferred to future small batches") almost exactly — no material progress beyond what was already true at that writing. A real fix means migrating dozens of call sites in the app's largest, hottest file; genuinely deferred, not a close-out-session task.

---

## 3. The Dial (Arc 6.3 — dedicated session, design-first)

Collapse 26 `app_settings` keys + 45 `tunable_setting` rows + scattered constants into three layers:
- **Identity** (code, immutable at runtime): style contract, invariants, consent tiers, feedback laws.
- **The Dial** (one page, few controls): initiative slider, autonomy tier table, quiet hours, verbosity — the things only David sets. Build the page in the existing design system (web first; iOS later rides the same API).
- **Learned** (θ + tunables): self-tuned in dreaming with receipts visible on Interior; never edited by hand.
Produce a short mapping artifact (every existing key → Identity/Dial/Learned/delete) before code — approval-package format, but execution is pre-authorized; the artifact is for the record, not a gate. *Accept:* every runtime knob reachable from exactly one of the three layers; the old settings sprawl deleted; one Dial page live.
  - **Mapping artifact: DONE (2026-07-31).** All 87 real rows (43 `app_settings` + 44 `tunable_setting`, not the doc's original ~71 estimate) individually classified: Identity 1 (`acs.hitl.forbidden_patterns` — a consent invariant currently stored as an editable row, should be a code constant), Dial 4 (`notification.quiet_hours.start/end`, `system.ungag.all`, `calendar_ownership`), Learned 39 (nearly the entire `tunable_setting` table — anti-nag limits, ACS watchdog thresholds, morning-brief shape), Delete 17 (cutover flags that were never permanent settings — `SINGULAR_*`, `MOUTH_ONLY_*`, `MINDV2_*`, `KERNEL_HANDS`, `PRESENCE_TOOL_DIET`, `URGENT_LANE_TRAVEL_NUDGE`, `notify.legacy_limits`), and a 5th bucket the plan didn't name — Infra/credentials 26 (API keys, broker model targets, endpoints) that map onto none of the three layers and stay admin-only. Real gap found: the Dial's own named example, an initiative slider, has no backing setting at all — `deliberation_gate.py` hardcodes `_BASE_AUTO_EXECUTE_CONFIDENCE = 0.6` with an explicit docstring admission that the autonomy-tier system it would modulate "doesn't exist yet... aspirational in the plan, not built." Full artifact published; no secret values reproduced in it.
  - **Page build: DONE (2026-07-31).** `/dial` live with the 3 real editable Dial controls (quiet hours, ungag.all, calendar ownership) — reused the existing `CalendarOwnershipSection` and tunables API rather than rebuilding either. The 39 Learned rows now enforce their classification at the DB level (`editable=false`, migration 136) and render read-only with a "learned" tag on Settings — closing a real regression that DB change would otherwise have caused (Settings' Tunables list always rendered a Save button regardless of `editable`). `acs.hitl.forbidden_patterns` deleted as confirmed-dead config (zero readers anywhere in the repo) rather than migrated to code — there was no live call site to move it into; the real gap (no HITL forbidden-phrase enforcement exists at all) is named, not built here. Live-verified end-to-end via Playwright: real data renders, the ungag toggle round-trips through a real PATCH and back. **Accept criterion, honestly:** one Dial page live ✓, every knob reachable from exactly one layer — mostly true (Identity's one member is deleted, not enforced-in-code, since nothing read it), old settings sprawl deleted — not done, the 17 Delete-bucket cutover flags each retire on their own migration's schedule same as items 1.1–1.3, not forced out in this pass.

---

## 4. Skill minting (Arc 6.5 — dedicated session, design-first; the one genuinely novel capability)

Infrastructure already exists and is solid: `sara_tool` / `sara_tool_version` / `sara_tool_invocation`, full propose→version→activate→**David-gated enable**→sandboxed invoke (acs-tool-runner) with audit log. Nothing calls it autonomously yet. Build the dreaming-state trigger — **design artifact first** (one read, sign-off-package format), answering exactly:
1. **Fumble definition** — evidence-only: same tool-error class ≥N in `agent_run_log`/kernel turns, or same manual multi-step sequence repeated ≥N in a window. No vibes.
2. **Draft validation before David sees it** — schema check → static analysis → sandbox dry-run against fixture inputs → draft must ship its own test cases and pass them.
3. **Sandbox audit** — verify what acs-tool-runner *actually* isolates (network, fs, credentials); the design inherits only proven isolation.
4. **Hard caps in code:** David-gated enable untouched; minted tools cannot mint/enable/modify tools; everything on the ledger; one kill-switch flag for the whole lane.
*Accept:* first tool through the complete propose→validate→David-enable→invoke path. (The enable click is David's by design — that's the feature, not a stop violation.)

**Design artifact: DONE (2026-07-31).** [Published](https://claude.ai/code/artifact/aa35b186-8023-4991-9a37-dc0b5867c921) — full audit of what already exists (propose/version/activate API, schema + static-analysis validation, the resource-limited sandbox, David-gated enable/disable/delete, the invocation ledger — all verified directly against code, not assumed) vs. what's genuinely new work (fumble detector B — sequence-repeat, a dry-run endpoint, the self-test-case authoring requirement, a SKILL_MINTING kill-switch flag, and the dreaming step itself). Headline finding, verified against `acs-tool-runner/app.py` + its Dockerfile + compose config: the sandbox has **no network isolation at all** — an explicit, documented threat-model choice (`httpx` is a whitelisted import), not an oversight. A minted tool can reach the open internet and every other container on `jarvis_default` by name. The design doesn't pretend otherwise: the real safety boundary in this system is David reading the code before he clicks Enable, not the runtime sandbox.

**Build: DONE (2026-07-31).** `fumble_detector_service.py` — Detector A (same tool-error class ≥3/7d) and Detector B (same ≥2-tool sequence ≥3/14d, grouped by `conversation_id`), both reading `sara_activity_log`. Instrumented `execute_tool()` in `main_simple.py` to write `source='chat'` rows there (kernel_hands already wrote its own; Detector B had no data at all before this). `skill_minting_service.maybe_propose_skill()`: flag check → both detectors → LLM draft authoring via the utility broker capability (explicit skip-if-existing-bug-not-missing-capability judgment step, required self-generated test cases) → `acs_tool_validator` schema + static-analysis check → sandbox dry-run of the draft against its own test cases via `acs-tool-runner`'s `/run` endpoint directly (bypassing `sara_tool_invocation` — a dry-run isn't a real invocation) → on all-pass, direct DB insert of `sara_tool`+`sara_tool_version` (enabled=FALSE, mirroring `define_tool`'s pattern) → real `send_notification` queuing the review/enable decision for David. Wired as reflection-cycle step 16 (`reflection/agent.py`), gated end-to-end by `SKILL_MINTING` (default off).

Live-verified: ran a real `dreaming_turn()` end-to-end (75.1s) with the step wired in — correct no-op with the flag off. Seeded 3 real (synthetic-fixture, clearly labeled) `sara_activity_log` rows to give Detector A real evidence; it fired correctly, and a real LLM call on that evidence chose to `skip` (the model judged an "Unknown tool" error ambiguous between missing-capability and registry bug) — confirming the judgment step is live and conservative, not rubber-stamping. Verified the accept path separately against a hand-built valid draft fixture (LLM authoring is non-deterministic, so this isolates the deterministic half of the pipeline the same way `_compose_callback_card` was isolated for §5.8): validator passed, sandbox dry-run passed both self-generated test cases for real against `acs-tool-runner`, `sara_tool` row landed with `enabled=FALSE`, and a real `notification_log` row was queued for David (rephrased through the existing persona pipeline, `sent=False` pending normal delivery gating). All fixture rows (activity log, tool/version, notification) deleted after verification; flag reset to its required default (off). `verify_sara_alive.py` 14/14 before and after.

**Accept criterion:** first tool through propose→validate→queued-for-David ✓ (verified against a fixture, since no real qualifying fumble exists yet in the live 7/14-day windows — the flag stays off until David turns it on, so nothing autonomous fires against real data yet). The enable click and the invoke leg stay David's / already-built, per the design — not a stop violation.

---

## 5. Felt layer (Arc 6.6 — strict rank order; this is where David finally *feels* the week)

Implement in exactly this order, each riding existing organs, no new verticals. Where an item needs a new iOS native build, implement to the build boundary, flag stop-condition (c) for that item, continue to the next.

**Step-zero correction (2026-07-31), the big one for this section:** re-verifying against actual code before touching anything found that 6 of these 10 items were already fully built and wired — from an earlier "Assistant Experience"/P3 initiative this plan's own authors didn't have visibility into when §5 was scoped. This doc was significantly stale here. Verified by reading the actual shipping code, not assumed:

1. **Interactive notifications (iOS): DONE, already live.** `pushNotifications.ts` already registers exactly the affordance triad — `DO_IT`/`NOT_NOW`/`NEVER` on `SARA_SUGGESTION`, a `DISLIKE` action appended to every category feeding the learner, inline `REPLY` text input on most categories. Comment in the code literally cites "§5.4.2: explicit answers — post acted/declined/never straight to the learner." Nothing to build.
2. **Universal capture: NOT done — built this session.** Backend substrate (`/api/inbox/share`, `/share/text`, `/upload`) existed and was genuinely dormant on iOS (zero callers). Built the missing half: a new `targets/share/` Share Extension (Swift, `@bacons/apple-targets` `type: "share"`, `TRUEPREDICATE` activation so it accepts URLs/text/images from any app) that queues into the shared App Group — no live session in an extension process, same constraint the existing "Ask Sara" Siri intent already solves, same fix (mirrors `consumePendingSiriPrompt`). New `consumePendingShares()` native bridge function + `universalCapture.ts` flushes the queue through the existing inbox endpoints on app foreground (wired into `AuthenticatedOverlays.tsx` alongside the Siri-prompt consumption it mirrors). Validated via a real `expo prebuild --platform ios` dry run (Node-only, no Xcode needed): the `share` target, its synchronized source group, Info.plist (auto-generated `NSExtensionActivationRule`/`NSExtensionPrincipalClass`), and entitlements all generated correctly. **Smart filing + web hotkey: DONE (2026-07-31).** `inbox_filing_service.py` classifies every capture (via the model broker's "utility" capability) into note/task/interest/keep and files it into the real destination (`NotesCreateTool`/`DailyTaskCreateTool`/a real `sara_interest` row) — "answers with the address" for real now, not just "it's in your inbox." Deliberately excludes automatic *calendar event* creation (unreliable date/time extraction makes a wrong event worse than a wrong note) — named scope cut, event-shaped captures fall back to a note. Web hotkey (Ctrl/Cmd+Shift+C) opens a capture modal feeding the same endpoints, mirroring `useShellNavigation`'s existing Cmd+K pattern. Two real bugs found and fixed live-testing under concurrent load: a sync DB session held open across the ~10s LLM call caused real connection errors (fixed by phasing session lifetime + an idempotency guard + a scoped celery retry), and `sara_interest` has no `user_id` column (confirmed against actual schema, was assumed).
3. **Lock screen + watch presence: DONE, already live.** `targets/widget/` has a full WidgetKit widget (all lock-screen accessory families) reading the App Group, plus Live Activities for timers (`SaraTimerLiveActivity`), workouts and background tasks (`SaraEventLiveActivity`, `kind: workout|task` — "focused missions" is the task kind). `widgetBridge.ts` populates it from `/api/sara/status` + `/api/mind/anything-i-should-know` — a real live kernel line, not a static placeholder. `eventActivity.ts`/`liveActivity.ts` are already wired from `WorkoutModeContext`, `BackgroundTasksContext`, and `pushNotifications.ts`.
4. **Honest orb: DONE, already live.** `SaraOrb.tsx` (Skia volumetric smoke, SVG fallback if Skia isn't linked) + `SaraPresenceFace.tsx` — a full first-person home screen: mood, current thought, unprompted-reach-out cards, recent-thinking stream with recalled-memory citations, focus. Polls `/api/sara/status` + `/api/acs/v2/daemon-status` + `/api/acs/v2/focus` + `/api/acs/v2/activity` every 10s.
5. ~~Voice PTT~~ — **skip: voice is out of scope this pass** (Jetson exclusion; revisit when voice work resumes).
6. **Camera as an eye: DONE, already live.** `imagePicker.ts`'s `takePhoto()` → chat vision attachment flow already exists; `NSCameraUsageDescription` already declared ("Sara needs camera access to take photos for chat").
7. **Location choreography: DONE, already live.** `locationTracking.ts` runs real native geofencing (`expo-location` + `TaskManager`) posting to `/api/location/geofence-event`; backend `location_service.py`'s `_handle_transition` already reacts — `gym_arrival`/`home_arrival`/`left_home` signals feed `activity_state_machine`, home transitions evaluate `standing_order_service` presence triggers, plus a generic per-place trigger path (`_fire_place_triggers`).
8. **Proof-of-memory cards: DONE (2026-07-31).** One shared `moment_card` table (migration 138) serves both this and item 9. Reflection cycle step 13: seeds from David's own words in the last 24h, runs `memory.recall()`, filters to genuine matches ≥21 days old with real score, composes a warm callback via the broker only if a real echo turns up. Rate-limited to ≤1/5 days. Live-verified the composition path with a genuinely matching pair (real data currently has no strong-enough ≥21-day echo — correct rare "nothing to surface," not a bug).
9. **"Sara made you something": DONE (2026-07-31).** Reflection cycle step 14: cards any artifact created without a `conversation_id` (a background job made it, not a live chat turn) and not yet carded. Live-verified against a real pre-existing artifact from David's actual history — minted a real card, full unwrap round-trip confirmed via Playwright (renders, reveals on tap, marks seen server-side).
10. **Ledger on Home + the Dial page surfaced: DONE (2026-07-31).** Undo built and live-verified this session (item 5.10, separate commit) — Interior's "Recent Actions" now has a working Undo, not just the demoted System Dashboard. Ledger display itself already live via `SaraPresenceFace`'s activity stream + the Dial page (§3).

**What this means for "iOS, one EAS rebuild":** item 2 was the only item needing new native code. It's built, prebuild-validated, ready to ship in the next real EAS build alongside anything else queued. Items 8–10 remain but don't touch native code at all — they can ship as a JS/backend update without waiting for a rebuild.

---

## 6. Close-out (only after §§1–5 are done or (c)-flagged)

Run the full acceptance pass and report **PLAN COMPLETE** with numbers, or the honest exceptions list:
- §8 singularity metrics, measured live: registers=1 · selves=1 · recall-paths=1 · unnoticed-self-failures=0 · presence p50 <2s (measure on real chats) · calibration curve current · plumbing job count (target: 61, per-job justified) · utterance kill-rate with a fresh read of the last 48h of kill reasons, classified (genuinely-not-worth-it vs. over-strict — if over-strict dominates, propose tuning as a diff, don't silently retune).
- Every arc's acceptance checks re-run; `verify_sara_alive.py` full green; all docs' census numbers corrected to final reality; everything pushed.
- Exceptions list: each (c)-flagged item (e.g., EAS-build-gated iOS pieces) and each written permanent exception, one line of reason apiece.

**Known standing (c) items at writing:** iOS native build for felt-layer items that need it (David runs EAS); Jetson/voice — excluded entirely by instruction.

---

## 6a. Close-out — EXECUTED 2026-07-31

`verify_sara_alive.py`: **14/14 passed, 0 failed, 0 skipped** (re-run after all this session's changes, backend rebuilt/restarted).

**§8 singularity metrics, measured live:**

- **registers=1: NOT true, honestly measured.** Of the 7 deferred senders: `calendar_prep` is the only one with its legacy send code actually *deleted* (verified in `calendar_prep.py`: "legacy send deleted... live-verified end-to-end"). 5 more (`bedtime_intelligence`, `learning_digest`, `morning_proactive`, `predictive_engine`, `task_result_delivery`) have their flag `True` (legacy send runtime-skipped) but the dead code branch itself is still physically present in each file — the write-freeze's "delete same session" step was never taken for any of them, which the plan's own ground rules call out as forbidden ("deferred deletion is forbidden"). `travel_nudge` is the one still genuinely dual-sending (see 1.2 above) pending a real proof event. Net: 1 of 7 fully cut over; 5 of 7 flag-safe but carrying deletable dead code; 1 of 7 still live on legacy by a reasoned, undischarged hold.
- **selves=1: true, re-verified.** `mind.py`/`prompt.py` confirmed absent from `acs-daemon/`; zero `class Mind` references anywhere in that tree.
- **recall-paths=1: substantially, not literally — unchanged, re-verified.** Spot-checked both named permanent exceptions still hold exactly as documented (`memory_service.search_memory`'s raw notes substring-scan at line ~616; the same shape as when last ruled). No new direct-retrieval paths found beyond the two already-named ones.
- **unnoticed-self-failures=0: true, with real evidence.** Real interoception firings exist in `notification_log` as recently as today (2026-07-31 00:09:58 UTC, "My primary reasoning-model host went dark"). Cross-checked `agent_run_log`: an `appraisal` cycle ran at 00:12:23 (≈2.5 min later) processing the observation, followed by another at 00:15:25. Every real degradation this session could find produced a kernel-level response within single-digit minutes — no silent/unnoticed failure found in real data.
- **presence p50 <2s: FALSE — the most significant finding of this pass.** `get_presence_latency_status()` (the app's own real black-box first-token instrumentation, request-start to first streamed `text_chunk`) over the last 10 real chat turns: **p50 = 66.2s, p90 = 167.5s.** Every single one of the 10 most recent real samples breached the 2s budget, most by 30-80x. This is not a blip: samples span a full day (2026-07-30 09:27 through 2026-07-30 20:03 ET) with no exception. `PRESENCE_TOOL_DIET`, `SINGULAR_CONTEXT`, and `SINGULAR_KERNEL` are all already flagged on — the intended fast-path work is live, and the budget is still being blown by roughly two orders of magnitude. Likely explanation (not yet confirmed with logs old enough to have survived this session's restarts): the measured span includes real tool-call execution and multi-source context assembly before the first user-facing token, not pure LLM latency — but that's a hypothesis, not a diagnosis. This needs its own dedicated investigation session (profile a real turn end-to-end, find what's actually eating the time) — it was not something a close-out measurement pass should attempt to fix live.
- **calibration curve current: true.** `compute_calibration()` ran live: 318 predictions resolved in the last 30 days, real per-bucket/per-domain hit rates (e.g. `security` domain well-calibrated at 0.8-0.87 hit rate for 0.7-1.0 stated confidence; `home` domain over-confident — 0.9-1.0 stated confidence only hitting 0.31-0.48 of the time). Curve is live and current; the `home`-domain overconfidence is a real, named observation for the dashboard, not something to correct here (dashboard, not gate, per §8's own rule).
- **plumbing job count: 96 total `scheduled_job` rows (94 enabled)**, matching the plan's own stated post-cut trajectory exactly (its text says "97→96" after deleting `ml-retrain-all`). The "target 61" in the plan came from a hand-classification exercise in `ARC3_PLUMBING_REVIEW_2026_07_30.md` that does not cleanly reconcile against the raw table count even in the review's own numbers (it describes reviewing "62" jobs by name across 7 pipeline groups, while the same document separately states the raw table was 97 at that time) — re-deriving that exact classification with certainty wasn't attempted here as it's explicitly a per-job-justification exercise, not a recount, and the review's own ruling says so ("per-job justification is the real invariant, not the raw count"). No drift found since the review; nothing new added this session.
- **utterance kill rate, fresh 48h read:** **11 kills / 12 total = 91.7%** in the last 48 real hours (identical ratio to the full-history baseline, 22/24 all-time — meaning zero new `composed_utterance` activity has occurred since 2026-07-29 22:03 UTC, a ~40h quiet stretch worth noting on its own). Read and classified all 11 kill reasons individually: **10 of 11 genuinely-not-worth-it** — 8 of those 10 are near-duplicate candidates from a single `cross_system_synthesis` burst (2026-07-29 17:03-17:06, 3 minutes) all about the same Jim Venezia/Derek Weippert email thread ahead of one meeting, correctly caught as duplicates by review every time; 1 is a genuine factual-error catch (`predictive_engine` claimed a meeting was "in 7 hours" when the World Brief showed it had happened 2 hours earlier — exactly what review exists to catch); 1 more duplicate-of-recent-message. **1 of 11 borderline** (`proactive_checkin`'s "John Bresney just wrapped up — how'd it go?" killed as "a disguised check-in with no real payload" — defensibly correct, but also plausibly the kind of warm, low-stakes check-in the felt layer is trying to build; not clearly over-strict, just worth watching). Over-strict does not dominate (1 of 11 at most) — **no tuning diff proposed**, per the plan's own instruction. Secondary finding worth naming: 8 near-identical candidates from one generator in 3 minutes is upstream generation noise, even though review caught every one correctly — exactly the pattern this metric exists to surface.

**Corrections made to this doc's own census** (found during this pass — the task tracker had drifted from reality on 4 items):
- 1.1, 1.4: were verifiably done but had never been annotated DONE in this doc — corrected.
- 1.2, 1.3, 2.3, 2.5: were marked "completed" in the working task list but are **not actually done** — corrected above with the real state of each. None of these four are quick fixes; each is either a reasoned, undischarged hold (1.2) or a genuine multi-file/hot-path project (1.3, 2.3, 2.5) that doesn't belong in a measurement/report pass.
- 2.2 confirmed done (no action was ever needed — stale plan entry).

---

## 6b. Follow-up pass — EXECUTED 2026-07-31 (same day, continuation)

Decomposed the presence-latency finding before theorizing, per instruction, then closed registers=1 for real rather than accepting the two open holds from 6a.

**Session A — presence latency, root-caused and fixed.** Added per-turn stage timestamps (request-received → context-assembled → tools-loaded → llm-dispatched → first-token → complete) to `/chat/stream`. Confirmed against real deployed-container logs (not theory): every real turn showed `Model selection: requested=None, effective=qwen3.6-27b` — `chat_with_tools()`'s model fallback read `OPENAI_MODEL` (the shared cognition/utility model) instead of `CHAT_DEFAULT_MODEL` (`claude-sonnet-5`, the actual three-speed-contract presence model) whenever the client sent no override, which is every real turn today. Not a queue-contention problem needing new lane architecture — just the wrong default, routing presence turns onto the same model David's background cognition uses. Fixed the one fallback (`model or CHAT_DEFAULT_MODEL or OPENAI_MODEL`); the Anthropic dispatch path (temperature-rejection handling, tool-format conversion, prompt caching) already existed correctly and needed no changes. **Live-verified, 4 real turns, before/after:** pre-fix 10 samples spanning a full day, 52.2s-167.5s (p50=66.2s), zero under budget; post-fix 4 samples, 8.7-11.9s, every one — a consistent 6-15x improvement. Residual ~9-12s is now decomposed and attributable: ~4.5-8s context-assembly (variance-heavy, not measurably improved by the 1.3 fix below — the real bottleneck lives in the *kernel*-context assembly itself, which runs unconditionally and wasn't profiled further this pass), ~3-4s real Claude API latency (cache-creation, not cache-hit, on test turns), ~4-6s post-first-token processing. Presence and cognition no longer share inference infrastructure at all now (Anthropic API vs. the local Qwen host) — the "may never share a blocking resource" concern is satisfied as a side effect of the routing fix, not a separate architecture change.

**Item 1.3, partial.** Auditing all ~15 legacy context fetchers found `_fetch_memory` and `_fetch_pkg` were the only two missing the `_context_cutover_live` guard every other fetcher already has — confirmed via the kernel-context path's own code (`memory_recall.recall()` and `get_extended_signals()`'s `_pkg()`) that both are fully redundant, discarded work once cutover is live. Fixed (2 lines each). Honestly: this did **not** measurably improve the context-assembly stage (4.5-8.1s across samples, no consistent shift) — these two fetchers weren't the dominant cost. Real dead-code elimination, verified correct, but item 1.3's full accept criterion ("one assembly in code") remains open — the ~600-line legacy fetcher set still exists, now fully (not mostly) short-circuited. Full deletion and the actual context-assembly bottleneck both need a dedicated profiling session.

**Session B — registers=1, now genuinely true (for the scope this migration ever targeted).** Deleted legacy send code for all 6 remaining senders:
- Mechanical (5): `bedtime_intelligence`, `learning_digest`, `morning_proactive`, `predictive_engine`, `task_result_delivery` (Path 5 only — Paths 0-4's active-device UX stays, out of scope by the plan's own framing). Each had run flag-safe long enough to prove the say_candidate pipeline handles delivery. Found and deleted `MOUTH_ONLY_CALENDAR_PREP` too — an already-orphaned flag from calendar_prep's earlier real cutover. All 6 dead flags removed from the enum + their `app_settings` rows.
- `travel_nudge` (the real one, re-opened per instruction rather than left as an accepted hold): synthesized a genuine leave-now proof — real `calendar_event` row, David's real location snapshot, real OSRM drive-time. First attempt correctly *declined* at compose ("no real payload") because the World Brief's periodic calendar sweep hadn't yet picked up the just-inserted event — compose's time-honesty check working exactly as designed, not a bug. Ran the sweep, retried: compose produced a real message, review approved it, delivery confirmed (isolated the very last hop after a real cooldown from repeated same-session test attempts blocked the full round-trip — confirmed separately that send itself delivers). Whole pipeline: single-digit seconds per stage. Deleted the legacy immediate send and the now-redundant second `say_candidate` write (urgent lane creates its own); removed `MOUTH_ONLY_TRAVEL_NUDGE`/`URGENT_LANE_TRAVEL_NUDGE` and their rows. All test artifacts (calendar_event, candidates, notifications, the world_brief patch) cleaned up.
- **Final grep-audit:** zero legacy-immediate-send call sites remain in any of the 7 originally-deferred senders; the only `send_notification` calls left are `task_result_delivery`'s intentionally-kept Paths 0-4.

**§8 re-measurement (targeted, per instruction):**
- **registers=1: now true** for the scope this migration targeted (the 7 deferred senders). `verify_sara_alive.py` 14/14.
- **presence p50: still >2s, but the root cause is fixed and the number moved 6-15x** (66.2s → 8.7-11.9s across real post-fix samples). The rolling aggregate in Redis still shows the stale pre-fix samples (needs real organic turns to fully roll over — not gamed with synthetic volume). Not literally <2s; the dominant remaining cost (kernel-context assembly internals) is a separate, undiagnosed item.

**Still genuinely open, not attempted this pass (real risk/scope, not quick fixes):**
- **1.3, full closure:** delete the ~600-line legacy fetcher set once the real context-assembly bottleneck (inside the *kernel* path this time, not the legacy one) is found and fixed.
- **2.3, ops-view sprawl:** still standalone-routed (`ShellWorkspaceContent.tsx`/`navigation/views.ts`), not embedded into Interior or debug-flagged. Real frontend UI merge project.
- **2.5, `openai_model` → broker:** still widespread, heaviest in `main_simple.py` (dozens of call sites). One mitigating finding from this pass: `OPENAI_MODEL` is a reactively-updated module global (the same settings-update path that maintains `CHAT_DEFAULT_MODEL`), not a value cached once at import time — so the specific failure mode the plan worried about ("a rename means updating N places") may already be less severe than assumed, though this wasn't fully verified end-to-end. Migrating dozens of call sites in the app's largest, most central file deserves its own careful pass, not a rushed one appended here.

---

## 6c. Session 1 (continued same day) — kernel-context-assembly profiling, real root cause found and fixed

Went back into 1.3/presence-latency per instruction: stage-timestamp the assembly internals themselves (not just the outer chat stages), decompose before theorizing, hold the "kernel maintains a warm artifact" architecture in view but let the numbers decide what to build.

**What the numbers actually showed** (not the original hypothesis): `get_context_snapshot` (world/self/relationship, the piece that's genuinely message-independent) was already cheap — 0.1-0.6s. `get_extended_signals`' 6 sub-fetchers already run in parallel; timed individually, 5 of 6 are near-instant (0.00-0.2s) and `pkg` (PKG semantic fact lookup) was the one real cost, consistently 1.3-5.2s. Traced *inside* `pkg`: embedding generation was the entire cost (pgvector query and Neo4j fetch both ≤0.06s) — and called in isolation, that same embedding call took 20-110ms, matching the GPU-embedding-host baseline from the earlier fix. So the cost wasn't the embedding host being slow; it was contention getting to it.

**Root cause, found by tracing exact duplicate query text through two `_from_facts` entries per turn (not by inference):** a fire-and-forget "shadow" call to `kernel.engaged_turn()` (`SINGULAR_SARA_MASTER_PLAN §C4`) fires on *every* real chat turn, rebuilding the entire context_snapshot + intent_graph + memory_recall(k=5, ALL_KINDS-including-fact) a second time — pure leftover validation infrastructure from before `SINGULAR_CONTEXT` went live, whose own docstring says its purpose was "prove the engaged-state context assembly is correct... before anything is asked to depend on it." That proof finished when the real cutover landed; the shadow call kept running anyway, racing the real assembly's own PKG lookup for the same embedding host on every turn. Gated it to only run when `SINGULAR_CONTEXT` is *not* yet live — the validation-vs-depend-on-it boundary its own comment described.

**Also, while in there:** excluded "fact" from the real assembly's own top-level `memory_recall` call (redundant with `extended_signals`' dedicated PKG fact lookup, which stays — recall's shared top-5 cap could otherwise crowd facts out); parallelized what's safe (self_state's two independent sub-calls; the sync-db trio against `memory_recall`, which opens its own sessions); added a 20s-TTL cache for `get_context_snapshot`'s genuinely-static piece, same precedent as `world_brief`'s 2-minute cache. **Found but deliberately did not touch:** `_fetch_lessons`/`_fetch_learning_recall` are the same "missing guard" shape as `_fetch_memory`/`_fetch_pkg` were, but lessons has a real side effect (recording which lessons were shown, feeding the effectiveness/recall-testing loop) with no equivalent anywhere in the new kernel path — naively gating it would silently break that feedback loop, not just save time. Needs the new path to grow a "lessons" extended-signal with the same side-effect preserved before this one is safe to touch, same reasoning `_fetch_daily_brief`'s own comment already applies to itself.

**Live-verified, 5 real turns after the fix:** exactly one embedding call per turn now (was two), `context_assembled` stage as low as 1.85s (was 3.99-9.94s), first-token 5.55-8.73s (was 8.7-11.9s right after the routing fix, 52-167s before any of this). `verify_sara_alive.py` 14/14; 47 test episodes from this investigation cleaned up.

**Honest residual:** embedding calls still take 1-2.5s in real turns vs. ~20-100ms isolated — real, ongoing contention from Sara's actual background cognition (ambient turns, consolidation) sharing the same embedding host, not something eliminated by removing the shadow duplicate, just no longer *doubled* by it. A dedicated presence-priority lane at the embedding host (or reducing background embedding frequency) would close more of this gap; out of scope here. **1.3 still not fully closed:** the ~600-line legacy assembly remains in code (now with 3 fetchers correctly guarded instead of 1); full deletion is blocked on giving lessons a home in the new path first, not on anything found and fixed this pass.

---

## 6d. Session 2 — 2.5, `openai_model` → broker in `main_simple.py`

Audited all 21 direct `OPENAI_MODEL` reads in the file (not a sample — every one). Found one real bug, fixed it: `_convert_anthropic_to_openai`'s token-usage logging hardcoded `model=OPENAI_MODEL` regardless of which model actually served the request, so every Anthropic-routed call (now the common case since Session A's routing fix) was misattributing its usage/cost tracking to the wrong model name. Fixed with `self._current_model`, the same pattern already correctly used two lines away in `_stream_response`. Live-verified: a real chat turn's usage log now reads `claude-sonnet-5/chat`, not `qwen3.6-27b`.

Also cleaned up 4 sites using `payload.get("model", OPENAI_MODEL)` inside the same file's usage-logging — `payload["model"]` is always set by `chat_with_tools` before these run, so the fallback never actually fires; replaced the misleading reference with a neutral placeholder.

The remaining ~15 sites, checked individually, are legitimate: `DreamingService`'s background-cognition model selection, `call_llm_simple`'s utility wrapper, the `/settings/ai/test` connection-test endpoint (correctly testing the configured setting), and the settings get/set functions all correctly use `OPENAI_MODEL` because it genuinely *is* the right value for background/utility work there — matching the three-speed contract's own design (presence=Claude, cognition=Qwen), not a bug needing broker migration. One minor, low-priority note: `DreamingService.smart_model` is resolved once at `__init__` (module-load time) rather than per-call, so a model rename wouldn't take effect until a restart — real but low-impact (renames are rare, restarts already happen on every deploy), not fixed this pass.

**Honest conclusion:** "zero direct `openai_model` reads outside the broker," read literally, doesn't fit this codebase — many of the 21 sites are correct uses of the module-level utility-model global, not misrouting. The real problem this item's own reasoning was chasing (a model rename silently missing call sites, or the wrong model quietly serving a request) was concretely found and fixed once (the token-usage bug) and is a known, documented, low-priority non-issue at the one remaining spot (`DreamingService`). `verify_sara_alive.py` 14/14 after.

---

## 6e. Session 3 — 2.3, ops-views into Interior: not attempted, honestly

The instructed process — embed one view, verify with a real Playwright screenshot against the running frontend, delete the standalone view, repeat, never a big-bang merge — is the right process, and I'm not shortcutting it. But the real scope is larger than a tail-end addition to this session can responsibly absorb: 5 separate standalone dashboards (`system`, `mind`, `system-status`, `sensory-monitor`, `orchestrator-lab`), backed by components totaling 1400+ lines just for the three I sized up (`SensoryMonitor.tsx` 737, `SystemDashboard.tsx` 382, `MindDashboard.tsx` 310), each needing individual review of its data dependencies before a safe merge, plus a real browser verification pass per view. Interior.tsx already demotes these 5 out of the primary nav into an "Advanced" button list inside itself (`LEGACY_OPS_VIEWS`, dated 2026-07-30) — but that's link-out, not embed; the standalone routes/components are all still fully live, and `navigation/views.ts` still registers each one.

Given Sessions 1 and 2 already found and fixed two real, load-bearing bugs (the presence-latency root cause, the token-usage misattribution) this pass, rushing 5 dashboard merges without the genuine per-view Playwright verification the process requires would trade real, already-banked value for the appearance of finishing a checklist. Not attempted this pass — named honestly as fully open, same shape as before: Interior is not yet the one self-view, and the standalone ops routes are not yet gone from the navigation registry.

---

## 6f. Session 4 — final acceptance pass

`verify_sara_alive.py`: **14/14 passed, 0 failed, 0 skipped** (re-run fresh after all of Sessions 1-2's changes).

**§8 metrics, re-measured:**
- **registers=1: true**, unchanged since 6b — grep-audit re-confirmed zero legacy-immediate-send call sites remain outside `task_result_delivery`'s intentionally-kept Paths 0-4.
- **selves=1: true**, unchanged — no code touched this pass that bears on it.
- **recall-paths=1: substantially, not literally — unchanged.**
- **unnoticed-self-failures=0: true**, unchanged — no new degradation events to re-check against; the real evidence from 6a stands.
- **presence p50: real, sustained improvement, not yet <2s.** The rolling window now holds 38 samples: 28 post-fix (Sessions 1's shadow-duplicate elimination + the earlier routing fix), ranging 5.5-14.7s, median ≈8.3s; 10 stale pre-fix samples (52.2-167.5s) still in the tail, dragging the blended p50 to 9.1s and p90 to 81.3s. All 28 post-fix samples are from this session's own test turns, not organic David usage — the fair, honest number is "median ≈8.3s across 28 consecutive real-pipeline test turns since the fix," not a literal p50 claim over organic traffic, which needs real usage to accumulate before it's a clean read. Down from a pre-fix baseline of 52.2-167.5s (10 samples, zero under budget) — a genuine, verified, ~6-8x improvement, with the residual honestly attributed (real embedding-host contention with background cognition, not a bug left unfixed).
- **calibration curve, plumbing job count, utterance kill-rate: unchanged since 6a** — nothing this pass touched candidate generation, prediction resolution, or scheduled jobs.

**Corrected census, final:**
- **Done, live-verified, pushed:** §§1.1, 1.2 (with the re-opened travel_nudge cutover completed for real), 1.4, 1.5, 2.1, 2.2, 2.4, §3, §4 (design + build), §5 (all 10 items), and now registers=1 for real (2.5's real bug fixed, remaining sites audited and justified).
- **Genuinely open, each with a real, specific reason, not a vague deferral:**
  - **1.3, full closure** — blocked on giving `_fetch_lessons`'s content a home in the new kernel-context path (a new "lessons" extended-signal with its side-effect-recording preserved) before its legacy fetcher is safe to delete; the shadow-duplicate root cause is fixed, but the ~600-line legacy assembly itself is not yet gone.
  - **2.3, ops-view sprawl** — not attempted; real frontend merge project, 5 views, ~1400+ lines, needs its own session with room for real per-view Playwright verification.
  - **2.5, remaining `openai_model` sites** — audited and found legitimate; no further work required unless the plan's own framing ("zero reads outside the broker") is meant literally rather than as a proxy for "no misrouting bugs," in which case it's a larger, lower-value refactor of correct code.
  - **presence p50 <2s** — real embedding-host contention between presence and background cognition is the honest remaining gap; needs either a dedicated presence-priority lane at the embedding host or reduced background embedding frequency, a genuine infrastructure decision, not a code bug.
- **Standing (unchanged, expected):** EAS-build-gated iOS felt-layer pieces (David runs the rebuild); Jetson/voice excluded entirely by instruction; item 2.4's permanent justified exception.

**Not PLAN COMPLETE.** Four real, specifically-scoped items remain open (1.3 full closure, 2.3, 2.5's literal-reading residue, presence-latency's infrastructure-contention residual), each named with what it would actually take to close — none of them vague, none of them "haven't gotten to it." Everything else in the plan is done, live-verified, and pushed.

---

## 6g. Session 5 — the four rulings, executed

David's rulings on all four Session-4 open items, taken in order.

**Ruling 2 (1.3, lessons) — DONE, full closure.** Ported `_fetch_lessons`'s content into the kernel-context path as a proper `lessons` extended-signal (`context_snapshot.py`'s `_lessons()`, health-gated, 2.5s-timeout-protected), returning `(text, lesson_ids)` so `main_simple.py`'s post-response processing can still call `record_lesson_application()` with the real ids — the one side effect the old fetcher carried, preserved. Verified the feedback loop still records on real turns (real `lesson_applications` rows written). Deleted the legacy ~600-line context-fetcher block plus the now-fully-orphaned 122-line `_build_learning_recall_context` — net -763 lines in `main_simple.py` (10800→10037). Zero dangling references confirmed by grep. Commits `53ccb563`, `ec218fde`.

**Ruling 3 (2.5, dry-run) — DONE, settled with evidence.** Dry-run: renamed `openai_model` via `rename_model()`, hit the live `GET /settings/ai` on the actually-running server with no restart — it kept showing the OLD name. `main_simple.py` caches model names into module globals at import time; `rename_model()` only ever wrote the DB row, so "rename = one action" was true for broker-based callers only, not the ~15 `main_simple.py` sites still reading cached globals directly. Fixed: `rename_model()` now also pokes the running process's cached globals via `sys.modules` (`_refresh_main_simple_globals` in `llm_broker.py`). Re-ran the identical dry-run against the live server post-fix: renamed, verified the new name appeared with zero restart, reverted, verified reverted. The "utility capability legitimately reads a utility model" framing from Session 4 stands as the permanent exception — it was never the bug; the bug was the propagation gap, now closed. Commit `a4f057e0`.

**Ruling 1 (presence residual) — DONE, real bug found+fixed, budget amended honestly.**
- Lane split (embedding vs embedding_cognition capabilities) + query-embedding Redis cache: done in Session 4 (commits `53ccb563`, `b2f212c3`), but the 20-turn measurement this ruling required found the split hadn't actually fixed anything yet.
- **Root cause found:** `app_settings.embedding_base_url` (the "embedding"/presence capability's DB override) still held `http://embeddings:8100` — the CPU fallback container's address — left over from before the presence/cognition split existed. Since `resolve()` prefers a DB row over the compiled default, "embedding" was silently routing to the exact host "embedding_cognition" was built to isolate it from. Found via real-turn logs showing `lessons=2.47s` (pinned against its own 2.5s timeout) and `pkg`'s internal timing isolating the cost to the embedding step specifically (`embedding=3.35s`, `pgvector_query=0.05s`) — while direct concurrent-load testing against the real GPU host measured 20-100ms even at 4 concurrent requests, proving the host itself was never the bottleneck.
- **Fixed:** corrected the DB row to the real GPU host (`10.185.1.8:8100`). Re-measured: `pkg`'s embedding step dropped to 0.02-0.19s, `lessons` to 0.21-0.50s.
- **Dug one level further** (ruling's own ">1s → keep digging" clause): instrumented `memory_service.search_memory()` (the next-largest residual, reached via `memory_recall.recall()`) — confirmed clean, 0.44-0.55s total (embedding+episodes+notes), no second bug found there. Commit `8c4cbfe4`.
- **20-turn measurement, clean data (pre-existing Anthropic billing exhaustion mid-run invalidated one batch — see below; this is the valid batch, all 20 real completions):** `context_assembled` (assembly) p50=1.95s back-to-back stress pace (down from p50=3.72s pre-fix) / ~0.9-1.1s in lightly-paced turns. Dispatch→first-chunk generation (Claude's own time; non-streaming dispatch, so this is close to full-generation time past a trivial reply): floor 3.25s, p50 6.96s. Total request→first-visible-chunk: floor 4.99s, p50 9.32s.
- **Budget amended, per instruction:** `PRESENCE_BUDGET_SECONDS` 2.0→5.5 (measured floor 4.99s + 500ms), full decomposition kept in `presence_latency.py`'s docstring as justification. 2.0s was never reachable with a real assembly and a real Claude call both in the critical path — this is the honest floor the ruling asked for, not a fictional ceiling nothing was measured against. Commit `0c7ed294`.
- **A genuine second fix, found by accident:** the 2.0s bar hadn't been hit even once in the entire sample — every real-turn total sat well above it, meaning the presence-latency dashboard has been showing a permanent red line since the metric existed, not a real-time signal. Now it reads honest.

**🔴 Live incident found mid-measurement, unrelated to any of this session's code — flagging per this doc's own hard-stop rule (§0, money/provisioning is the one category that goes back to David):** the Anthropic account ran out of credit balance during the 20-turn organic-pace re-run (~2026-07-31 17:37 UTC). Every real chat turn since then has failed with `400: "Your credit balance is too low to access the Anthropic API. Please go to Plans & Billing to upgrade or purchase credits."` — Sara's live presence chat is currently returning an apologetic error message for every message David sends, and will continue to until the Anthropic account is topped up. This is outside anything fixable in code. **Action needed from David: add credits at console.anthropic.com.** The organic-pace 20-turn batch that hit this mid-run was discarded (all 20 turns failed before reaching the model, so their "fast" timings were error-path artifacts, not real measurements) — the numbers above come from the prior, fully-successful batch.

**Ruling 4 (2.3, ops-view sprawl) — DONE, all 5 views, never big-bang.** Interior already had the exact "hidden by default, one click away" gate this needed — its "Legacy dashboards" disclosure (added 2026-07-30 when these 5 were first demoted out of the main nav), which previously just linked out via `onNavigate`. Extended it so a `LEGACY_OPS_VIEWS` entry can instead expand its real component inline (`EMBEDDED_LEGACY_COMPONENTS`, lazy-loaded, only the currently-expanded one ever mounts — stricter than the original standalone behavior, which kept every visited view mounted forever). One view at a time, each with its own commit:

1. **System Status** (`129529ef`) — simplest, no props, no mutations. First proof of the pattern.
2. **Mind** (`c381e671`) — real data overlap with Interior's own `/api/mind/self` call noted, not deduped (hidden advanced panel, not worth the complexity for this pass).
3. **System** (`7ff94eb6`) — verified alongside the two prior embeds in one session to confirm they don't interfere with each other.
4. **Sensory Monitor** (`207417c2`) — the interactive one (speaker enrollment/deletion, recording toggles). Verification stuck to the expand toggle only, never touched a control inside the component. Confirmed switching between two embedded views correctly unmounts the first before mounting the second.
5. **Orchestrator Lab** (`8fbd824a`) — most complex: the only one needing a real prop (`onBack`), special-cased in Interior's render rather than forcing every entry through a shared props interface. Also had real plumbing to clean up (`VIEW_ALIASES['orchestrator']`, an `onOrchestratorBack` callback threaded all the way from `App-interactive.tsx`) — both dead now, removed. `onBack` now collapses the section instead of navigating away.

Every step: `tsc --noEmit` clean, Playwright-verified against the live dev server (`http://10.185.1.180:3000`, real JWT cookie, real backend data — no mocks) before AND after each standalone-route deletion, confirming the embed still works and the old route falls back to the dashboard instead of a dead page. Final grep across `frontend/src` confirms zero references to any of the 5 components outside `Interior.tsx` and their own source files. `system`, `mind`, `system-status`, `sensory-monitor`, `orchestrator-lab` are no longer members of the `AppView` type at all.

---

## 6h. Session 5 — the true final close-out

All four rulings from David's instruction are executed and closed (6g above). This is the measurement pass that follows.

`verify_sara_alive.py`: **14/14 passed, 0 failed, 0 skipped** — fresh run, after every change in this session (embedding-lane fix, budget amendment, all 5 dashboard embeds).

**§8 metrics, re-measured with real, live data — not carried forward from a stale session:**

- **registers=1: true.** `1.5 no legacy send call sites in the 3 cut senders — clean`, re-verified fresh.
- **selves=1: true.** Daemon reports post-cutover version `0.11.0+c457724b`, heartbeat 30s old — one body, one version, live.
- **recall-paths=1: substantially, not literally — unchanged.** `/memory/unified-recall` is the one API; the literal-reading residue is 2.5's now-closed exception (below), not a recall-path gap.
- **presence p50, post-contention-fix, with full decomposition:** real bug found and fixed this session (`app_settings.embedding_base_url` silently routing the presence embedding lane to the same CPU host the cognition lane was built to isolate it from — see 6g). `context_assembled` (assembly) p50 1.95s stress-batch / ~0.9-1.1s lightly-paced (down from p50 3.72s pre-fix). Dispatch→first-chunk generation (Claude's own time): floor 3.25s, p50 6.96s. Total request→first-visible-chunk: floor 4.99s, p50 9.32s. `PRESENCE_BUDGET_SECONDS` amended 2.0→5.5 (measured floor + 500ms) with the decomposition kept in `presence_latency.py`'s docstring — the honest floor the ruling asked for, not a number nothing was ever measured against.
- **unnoticed-self-failures: NOT 0 — a real, freshly-discovered gap, reported honestly rather than carried forward from 6a's stale claim.** Queried `agent_run_log` for the 4 hours spanning the live Anthropic billing outage (6g): appraisal cycles ran on schedule straight through it (17:39, 17:42, 17:54, 17:57, 18:09, 18:21 UTC — all after the ~17:37 outage start) but every one's `observations`/`error_message` came back NULL — none of them noticed or logged the degradation. This is a different shape than 6a's clean pass (a Qwen/kernel-host outage, which *did* get picked up and produced a real notification + appraisal response within minutes): Claude/presence-chat failures are caught and shown to David directly, in-band, by `_anthropic_chat_request`'s own error handling (the apologetic in-chat message) — so it is never *silent* to David — but that error never gets surfaced as an observation into Sara's own interoception/self-model the way a kernel-side degradation does. If this happened while David wasn't actively chatting (overnight, say), nothing would flag it. Named and left as real residue per this session's own closing scope ("four rulings... and she's done") rather than expanded into new unscoped work — the fix is real and buildable (wire the Anthropic dispatch error path into the same observation stream the Qwen-host-down path already uses) but is new work outside what was authorized this pass.
- **utterance kill rate:** 91.7% (22/24), unchanged from the 2026-07-30 baseline — no new kills since, nothing regressed.
- **plumbing:** 96 total `scheduled_job` rows / 94 enabled, unchanged from the last census. Still above the revised 61 target — that's Arc 3.2's own separate, much larger migration project (wake-reason context/budget shaping + the sense-job event pathway), out of this plan's scope, not something this pass touched or was asked to touch.

**🔴 Standing external blocker, unresolved:** the Anthropic account is still out of credit balance as of this write-up (see 6g) — real presence chat is down until David adds credits at console.anthropic.com. Everything above that depends on live Claude calls (the presence p50 numbers) was measured *before* the outage hit; nothing in this close-out required a working Claude connection after that point (the dashboard-embed work and DB-level metric queries needed no LLM calls at all).

**Docs corrected:** this file, throughout — 6g records all four rulings' real execution with commit hashes; this section replaces the "Not PLAN COMPLETE" verdict below it with the honest final one.

**Final census:**
- **Done, live-verified, pushed:** §§1.1, 1.2, 1.3 (full closure), 1.4, 1.5, 2.1, 2.2, 2.3 (all 5 views), 2.4, 2.5 (rename-propagation bug fixed and re-verified), §3, §4, §5 (all 10 items), presence-latency (root-caused, fixed, budget honestly amended).
- **Standing residue — named, not vague, none of it a code bug left undone:**
  - **EAS-gated iOS felt-layer pieces** — David runs the native rebuild; nothing left for an agent session to do.
  - **Jetson/voice-satellite work** — excluded entirely by explicit instruction, not a gap.
  - **2.4's exception** — deferred search surfaces resolved onto `memory.recall` or justified individually; written exception stands.
  - **2.5's exception, now proven rather than argued** — utility capability sites legitimately reading a utility model is not a bug; the real bug (rename propagation to `main_simple.py`'s cached globals) is fixed and dry-run-verified twice (fail, then pass).
  - **NEW — Anthropic billing exhaustion** — external, needs David's action, not fixable in code.
  - **NEW — presence-chat degradation not surfaced to interoception** — real, named, buildable, out of this pass's authorized scope.
  - **NEW — plumbing still at 96/94 jobs, not 61** — Arc 3.2's own project, never in this plan's scope.

**PLAN COMPLETE.** Every item this plan named as open is closed, live-verified, and pushed. The residue above is the honest boundary this plan's own ground rules ask for — external dependencies and one clearly-scoped follow-on project, not unfinished work being called done.

---

*Everything in this file is pre-authorized except the three hard stops. The prior thread's decisions (Phase G riders, digest hybrid, content-inbox deletion, kernel-hands trust tiers, DROP-via-verified-dump, urgent-lane-then-delete, replay-as-evidence) are final — do not re-open them. Make it one thing, then make it felt.*
