# Sara Desktop Sidecar Dependencies

# Activity monitoring
pynput>=1.7.6

# Screenshot capture
mss>=9.0.0
Pillow>=10.0.0

# Backend communication
websockets>=12.0
httpx>=0.26.0

# System metrics
psutil>=5.9.0

# Desktop actuators (clipboard, window focus). pynput above also handles keystrokes.
pyperclip>=1.8.2
pygetwindow>=0.0.9

# Voice module: local TTS playback, mic capture (A3/A6)
sounddevice>=0.4.6
numpy>=1.26.0

# macOS permission detection (A8) — Screen Recording, Accessibility, Microphone
pyobjc-core>=10.1; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.1; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=10.1; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=10.1; sys_platform == "darwin"

# Windows media-session detection (A6/B2.4) — "is music/video playing" for
# the Jetson's ambient wake-threshold boost. winsdk has no stable 1.0.0 (only
# 1.0.0bN pre-releases exist), and `>=1.0.0` excludes pre-releases — so pin to
# the beta explicitly, which also opts pip into pre-releases for this package.
winsdk>=1.0.0b10; sys_platform == "win32"

# For packaging
pyinstaller>=6.0.0
