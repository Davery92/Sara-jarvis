# Voice Control Dependencies for Shadow Agent
# Audio foundation
openwakeword>=0.6.0
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0

# TFLite runtime for wake word models
tflite-runtime>=2.14.0; platform_system != "Windows"
# Windows users: use onnxruntime instead (tflite-runtime not available on Windows)
onnxruntime>=1.16.0

# VAD (Voice Activity Detection)
torch>=2.0.0
torchaudio>=2.0.0
silero-vad>=4.0.0

# Wyoming Protocol
websockets>=12.0
aiohttp>=3.9.0

# Audio processing
pydub>=0.25.1
webrtcvad>=2.0.10

# Existing dependencies (already in main requirements.txt)
requests>=2.31.0
psutil>=5.9.0
